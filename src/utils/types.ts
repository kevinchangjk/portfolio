export interface Tech {
  name: string;
  imageUrl: string;
  sourceUrl: string;
}

export interface Project {
  title: string;
  description: string;
  imageUrl: string;
  sourceUrl: string;
  previewUrl: string;
}
